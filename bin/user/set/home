#!/bin/sh

set -v # Turn on verbose logging, so that the following will present a tutorial.

# array(s) module api is loaded with the line 'api/array' in shell/includes.

# To set a user's home, note that we are not running this command as it affects
# the state of the system (disabled by prepending 'echo').

echo user set home /home/example user example

