#!/bin/sh

set -v # Turn on verbose logging, so that the following will present a tutorial.

# array(s) module api is loaded with the line 'api/array' in shell/includes.

# We can check if the current user is root, and act on the result
if user exists name example
then
  log "User 'example' exists! "
else
  log "User 'example' does not exist! "
fi

