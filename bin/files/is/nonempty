#!/bin/sh

set -v # Turn on verbose logging, so that the following will present a tutorial.

# file(s) module api is loaded with the line 'api/files' in shell/includes.

# We can test if a file is nonempty and act according to the result:
if file is nonempty /etc/profile
then
  log "/etc/profile is nonempty :)"
else
  log "/etc/profile is empty :("
fi

