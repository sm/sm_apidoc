#!/bin/sh

set -v # Turn on verbose logging, so that the following will present a tutorial.

: # Load the array module API, this is normally done in the shell/includes
: # file as simply 'api/files' but is done explicitly here for demonstration.
includes api/files

: # We can test if all files given are symlinks and act according to the result:
if files are symlinks /etc/profile ~/.bashrc
then
  log "/etc/profile and ~/.bashrc are both symlinks :)"
else
  log "/etc/profile and/or ~/.bashrc are not symlinks :("
fi

