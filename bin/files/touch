#!/bin/sh

set -v # Turn on verbose logging, so that the following will present a tutorial.

# file(s) module api is loaded with the line 'api/files' in shell/includes.

# Touch a file
file touch /tmp/file_touch_example

# Let's checkout the file and it's contents,

ls -l /tmp/file_touch_example

cat /tmp/file_touch_example

# Clean up after ourselves.
file remove /tmp/file_touch_example


